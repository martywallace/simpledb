language: php
php:
  - '5.6'
  - '7.0'
  - '7.1'
services:
  - mysql
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'
  - mysql -e 'USE test; CREATE TABLE users (id int(10) UNSIGNED NOT NULL, name varchar(64) COLLATE utf8_unicode_ci DEFAULT NULL, email varchar(128) COLLATE utf8_unicode_ci DEFAULT NULL) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;'
  - mysql -e 'USE test; TRUNCATE users;'
install:
  - composer install